%h1 Listing geometries

%table
  %tr
    %th Room
    %th Geometry
    %th
    %th
    %th

  - @geometries.each do |geometry|
    %tr
      %td= geometry.room_id
      %td= geometry.geometry
      %td= link_to 'Show', geometry
      %td= link_to 'Edit', edit_geometry_path(geometry)
      %td= link_to 'Destroy', geometry, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Geometry', new_geometry_path
